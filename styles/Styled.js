import { Box } from "@mui/material";
import { styled } from "@mui/system";

const ArrowContainer = styled(Box)({
    position:"absolute",
    height:"20%",
    widows:"10%",
    margin:"auto",
    top:0,
    bottom:0,
    backgroundColor:"red"
})

export {
    ArrowContainer
}